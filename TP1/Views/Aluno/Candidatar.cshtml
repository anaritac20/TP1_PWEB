@model TP1.Models.Proposta

@{
    ViewBag.Title = "Candidatar";
}

<h2>Candidatar</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Aluno</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            @Html.LabelFor(model => model.Titulo, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
              @*  @Html.DropDownListFor(m => m.Titulo, (SelectList)ViewBag.Propostas, "Selecione Proposta", new { @class = "form-control" })*@
                @Html.DropDownList(Model.Titulo)
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Titulo, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DropDownListFor(m => m.Titulo, (SelectList)ViewBag.Propostas, "Selecione Proposta", new { @class = "form-control" })
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
